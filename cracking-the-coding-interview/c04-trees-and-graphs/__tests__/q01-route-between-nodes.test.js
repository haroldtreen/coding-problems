const solutionInfo = require('../../test-helpers').solutionInfo(__filename);
const funcs = require(solutionInfo.filename);

describe(`${solutionInfo.name}: todo`, () => {
  it('a test', () => {
    // TODO
  });
});
